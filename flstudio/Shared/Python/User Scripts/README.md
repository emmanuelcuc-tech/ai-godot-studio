# User Scripts (piano roll / Edison)

Copy this folder’s `.py` files into FL Studio:

`Shared\Python\User Scripts\`

- `SetAllVelocities.py` — set every piano-roll note to velocity 0.8
- `EdisonReady.py` — Edison `enveditor` handshake

See `flstudio/SKILL.md` for the three scripting contexts.
